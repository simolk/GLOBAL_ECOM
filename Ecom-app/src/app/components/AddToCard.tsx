'use client';
const AddToCard = () => {
    return(
        <div>
            <button className="btn btn-primary-content" onClick={() => console.log('clicked on Add to card button')}>Add-To-Card</button>
        </div>
    )
}

export default AddToCard